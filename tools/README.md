# Tools

| Path | Role |
|------|------|
| `profiling/` | Profiling helpers. |
| `bin/` | Old standalone test binaries (not the CMake `build/` solvers). |
| `packages/` | Local `.deb` leftovers; not part of the solver. |
