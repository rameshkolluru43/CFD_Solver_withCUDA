# Tests

| Path | Role |
|------|------|
| `Test_Cases/` | 2D case drivers compiled into the 2D solver (`Half_Cylinder_Test_Case.cpp`, bump, SWBLI, …). |
| `Unit_Test_Codes/` | Standalone unit / GMSH reader tests. |
| `examples/` | Small integration examples. |
| `debug_scratch/` | One-off viscous debug binaries/sources (not part of the CMake targets). |
