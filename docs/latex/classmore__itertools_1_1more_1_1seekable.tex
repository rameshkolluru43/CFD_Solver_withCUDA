\doxysection{more\+\_\+itertools.\+more.\+seekable Class Reference}
\hypertarget{classmore__itertools_1_1more_1_1seekable}{}\label{classmore__itertools_1_1more_1_1seekable}\index{more\_itertools.more.seekable@{more\_itertools.more.seekable}}
\doxysubsubsection*{Public Member Functions}
\begin{DoxyCompactItemize}
\item 
\Hypertarget{classmore__itertools_1_1more_1_1seekable_af4edb2118dc52725a3fdd71f6f7a8124}\label{classmore__itertools_1_1more_1_1seekable_af4edb2118dc52725a3fdd71f6f7a8124} 
{\bfseries \+\_\+\+\_\+init\+\_\+\+\_\+} (self, iterable, maxlen=None)
\item 
\Hypertarget{classmore__itertools_1_1more_1_1seekable_ae29f0fb287245d691e4d919a59808ba3}\label{classmore__itertools_1_1more_1_1seekable_ae29f0fb287245d691e4d919a59808ba3} 
{\bfseries \+\_\+\+\_\+iter\+\_\+\+\_\+} (self)
\item 
\Hypertarget{classmore__itertools_1_1more_1_1seekable_a2c3452140c2fd6b296e449f94de84bb3}\label{classmore__itertools_1_1more_1_1seekable_a2c3452140c2fd6b296e449f94de84bb3} 
{\bfseries \+\_\+\+\_\+next\+\_\+\+\_\+} (self)
\item 
\Hypertarget{classmore__itertools_1_1more_1_1seekable_a147618df173c6764076d6628c175c318}\label{classmore__itertools_1_1more_1_1seekable_a147618df173c6764076d6628c175c318} 
{\bfseries \+\_\+\+\_\+bool\+\_\+\+\_\+} (self)
\item 
\Hypertarget{classmore__itertools_1_1more_1_1seekable_a9329b601b2eab9569ce7f10b46a2484b}\label{classmore__itertools_1_1more_1_1seekable_a9329b601b2eab9569ce7f10b46a2484b} 
{\bfseries peek} (self, default=\+\_\+marker)
\item 
\Hypertarget{classmore__itertools_1_1more_1_1seekable_abc1f407e7de8db76e827b815e7cf87ef}\label{classmore__itertools_1_1more_1_1seekable_abc1f407e7de8db76e827b815e7cf87ef} 
{\bfseries elements} (self)
\item 
\Hypertarget{classmore__itertools_1_1more_1_1seekable_a9c992d1cc50d6e86b0994af3bdb6763d}\label{classmore__itertools_1_1more_1_1seekable_a9c992d1cc50d6e86b0994af3bdb6763d} 
{\bfseries seek} (self, index)
\item 
\Hypertarget{classmore__itertools_1_1more_1_1seekable_a319913afc74222e2fdf69dd872287bb0}\label{classmore__itertools_1_1more_1_1seekable_a319913afc74222e2fdf69dd872287bb0} 
{\bfseries relative\+\_\+seek} (self, count)
\end{DoxyCompactItemize}
\doxysubsubsection*{Public Attributes}
\begin{DoxyCompactItemize}
\item 
\Hypertarget{classmore__itertools_1_1more_1_1seekable_a45c8f5745fc583795f112a37b9a44cd8}\label{classmore__itertools_1_1more_1_1seekable_a45c8f5745fc583795f112a37b9a44cd8} 
int {\bfseries \+\_\+index} = 1
\end{DoxyCompactItemize}
\doxysubsubsection*{Protected Attributes}
\begin{DoxyCompactItemize}
\item 
\Hypertarget{classmore__itertools_1_1more_1_1seekable_ab4a12065ea0c355f1843beeb459ab937}\label{classmore__itertools_1_1more_1_1seekable_ab4a12065ea0c355f1843beeb459ab937} 
{\bfseries \+\_\+source} = iter(iterable)
\item 
\Hypertarget{classmore__itertools_1_1more_1_1seekable_a8b132aace7d6192f4d937ae7b12159e9}\label{classmore__itertools_1_1more_1_1seekable_a8b132aace7d6192f4d937ae7b12159e9} 
list {\bfseries \+\_\+cache} = \mbox{[}$\,$\mbox{]}
\item 
\Hypertarget{classmore__itertools_1_1more_1_1seekable_a45c8f5745fc583795f112a37b9a44cd8}\label{classmore__itertools_1_1more_1_1seekable_a45c8f5745fc583795f112a37b9a44cd8} 
{\bfseries \+\_\+index} = None
\end{DoxyCompactItemize}


\doxysubsection{Detailed Description}
\begin{DoxyVerb}Wrap an iterator to allow for seeking backward and forward. This
progressively caches the items in the source iterable so they can be
re-visited.

Call :meth:`seek` with an index to seek to that position in the source
iterable.

To "reset" an iterator, seek to ``0``:

    >>> from itertools import count
    >>> it = seekable((str(n) for n in count()))
    >>> next(it), next(it), next(it)
    ('0', '1', '2')
    >>> it.seek(0)
    >>> next(it), next(it), next(it)
    ('0', '1', '2')
    >>> next(it)
    '3'

You can also seek forward:

    >>> it = seekable((str(n) for n in range(20)))
    >>> it.seek(10)
    >>> next(it)
    '10'
    >>> it.relative_seek(-2)  # Seeking relative to the current position
    >>> next(it)
    '9'
    >>> it.seek(20)  # Seeking past the end of the source isn't a problem
    >>> list(it)
    []
    >>> it.seek(0)  # Resetting works even after hitting the end
    >>> next(it), next(it), next(it)
    ('0', '1', '2')

Call :meth:`peek` to look ahead one item without advancing the iterator:

    >>> it = seekable('1234')
    >>> it.peek()
    '1'
    >>> list(it)
    ['1', '2', '3', '4']
    >>> it.peek(default='empty')
    'empty'

Before the iterator is at its end, calling :func:`bool` on it will return
``True``. After it will return ``False``:

    >>> it = seekable('5678')
    >>> bool(it)
    True
    >>> list(it)
    ['5', '6', '7', '8']
    >>> bool(it)
    False

You may view the contents of the cache with the :meth:`elements` method.
That returns a :class:`SequenceView`, a view that updates automatically:

    >>> it = seekable((str(n) for n in range(10)))
    >>> next(it), next(it), next(it)
    ('0', '1', '2')
    >>> elements = it.elements()
    >>> elements
    SequenceView(['0', '1', '2'])
    >>> next(it)
    '3'
    >>> elements
    SequenceView(['0', '1', '2', '3'])

By default, the cache grows as the source iterable progresses, so beware of
wrapping very large or infinite iterables. Supply *maxlen* to limit the
size of the cache (this of course limits how far back you can seek).

    >>> from itertools import count
    >>> it = seekable((str(n) for n in count()), maxlen=2)
    >>> next(it), next(it), next(it), next(it)
    ('0', '1', '2', '3')
    >>> list(it.elements())
    ['2', '3']
    >>> it.seek(0)
    >>> next(it), next(it), next(it), next(it)
    ('2', '3', '4', '5')
    >>> next(it)
    '6'\end{DoxyVerb}
 

The documentation for this class was generated from the following file\+:\begin{DoxyCompactItemize}
\item 
Python\+\_\+\+Wrapper/\+My\+Python\+Env311/lib/python3.\+11/site-\/packages/setuptools/\+\_\+vendor/more\+\_\+itertools/more.\+py\end{DoxyCompactItemize}
